ruby:
  case type
  when "signup"
    then link_text = "Sign up"
  when "login"
    then link_text = "Login"
  when "connect"
    then link_text = "Connect"
  end

a.facebook-link href="#{user_omniauth_authorize_path(:facebook)}"
  i.fa.fa-facebook-square.fa-2 style="margin-right: 10px;"
  - if type == "signup"
    | Facebook
  - else
    = "#{link_text} with Facebook"