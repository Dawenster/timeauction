<!DOCTYPE html>
<html lang="en">
  <head>
    <%= favicon_link_tag %>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <% 
      title = "Time Auction ⋅ We Reward Volunteers"
      description = "We let users trade volunteer hours to meet amazing people.  We believe volunteerism can improve our communities and we make it more enticing to do so."
    %>

    <% meta :title => title, :description => description, :keywords => %w(time auction volunteer reward meet amazing people) %>

    <% url = hk_domain? ? "http://www.timeauction.hk/" : "http://www.timeauction.org/" %>

    <% 
      meta [:property => "og:url", :content => url]
      meta [:property => "og:title", :content => title]
      meta [:property => "og:description", :content => description]
      meta [:property => "og:image", :content => "https://s3-us-west-2.amazonaws.com/timeauction/nicaragua_volunteering.jpg"]
      meta [:property => "og:type", :content => "website"]
      meta [:property => "fb:admins", :content => "81007543"]
      meta [:property => "fb:app_id", :content => facebook_app_id]
    %>

    <%= metamagic %>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <meta name="google-site-verification" content="vPS2tfsmRiI732kCPNAXSg_EqhaYjyO9D7CQ479HSQo" />
  </head>

  <body>
    <div id="wrap">
      
      <%= render "layouts/nav", :content => yield %>

      <%= render "layouts/facebook" %>
      <%= render "layouts/google_analytics" %>
      <%= render "layouts/twitter" %>

      <%= javascript_include_tag "application" %>

      <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/jquery-ui.min.js"></script>

      <%= stylesheet_link_tag "//ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/themes/cupertino/jquery-ui.min.css" %>
    </div>
    <%= render "layouts/footer" %>
    <%= switch_user_select %>
  </body>
</html>